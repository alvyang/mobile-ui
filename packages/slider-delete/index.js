import MoSliderDelete from './src/slider-delete.vue';

/* istanbul ignore next */
MoSliderDelete.install = function(Vue) {
  Vue.component(MoSliderDelete.name, MoSliderDelete);
};

export default MoSliderDelete;
